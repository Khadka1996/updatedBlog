import { createPageMetadata } from '../seo';
export const metadata = createPageMetadata({ title: 'Cookies Policy', description: 'Learn how Everestkit uses cookies and similar technologies.', path: '/cookies-policy' });
export default function Layout({ children }) { return children; }
