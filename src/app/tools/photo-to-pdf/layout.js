import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Photo to PDF Converter', description: 'Convert images and photos into PDF documents online.', path: '/tools/photo-to-pdf' });
export default function Layout({ children }) { return children; }
