import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'PDF to PowerPoint Converter', description: 'Convert PDF documents into PowerPoint presentations online.', path: '/tools/pdf-to-powerpoint' });
export default function Layout({ children }) { return children; }
