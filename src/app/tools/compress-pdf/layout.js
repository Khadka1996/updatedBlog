import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Compress PDF Online', description: 'Reduce PDF file size online while keeping documents easy to share.', path: '/tools/compress-pdf' });
export default function Layout({ children }) { return children; }
