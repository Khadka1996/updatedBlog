import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Online Calculator', description: 'Use the Everestkit online calculator for quick everyday calculations.', path: '/tools/calculator' });
export default function Layout({ children }) { return children; }
