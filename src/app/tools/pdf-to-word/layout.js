import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'PDF to Word Converter', description: 'Convert PDF documents into editable Word files online.', path: '/tools/pdf-to-word' });
export default function Layout({ children }) { return children; }
