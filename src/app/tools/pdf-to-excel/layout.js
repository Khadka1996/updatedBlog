import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'PDF to Excel Converter', description: 'Convert PDF tables and documents to Excel files online.', path: '/tools/pdf-to-excel' });
export default function Layout({ children }) { return children; }
