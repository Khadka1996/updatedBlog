import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Video Downloader', description: 'Download supported direct media files with the Everestkit video downloader.', path: '/tools/video-downloader' });
export default function Layout({ children }) { return children; }
