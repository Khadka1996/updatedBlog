import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Merge PDF Files Online', description: 'Combine multiple PDF files into one document with Everestkit.', path: '/tools/merge-pdf' });
export default function Layout({ children }) { return children; }
