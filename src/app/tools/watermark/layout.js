import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Image Watermark Tool', description: 'Add watermarks to images online with the Everestkit watermark tool.', path: '/tools/watermark' });
export default function Layout({ children }) { return children; }
