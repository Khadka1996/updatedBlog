import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Word to PDF Converter', description: 'Convert Word documents into PDF files online.', path: '/tools/word-to-pdf' });
export default function Layout({ children }) { return children; }
