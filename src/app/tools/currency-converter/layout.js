import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Currency Converter', description: 'Convert currencies quickly with the Everestkit online currency converter.', path: '/tools/currency-converter' });
export default function Layout({ children }) { return children; }
