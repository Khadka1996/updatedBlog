import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Date Converter', description: 'Convert and compare dates with the Everestkit online date converter.', path: '/tools/date-converter' });
export default function Layout({ children }) { return children; }
