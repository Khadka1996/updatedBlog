import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Photo Stretcher', description: 'Resize and stretch photos online with the Everestkit image tool.', path: '/tools/photo-stretcher' });
export default function Layout({ children }) { return children; }
