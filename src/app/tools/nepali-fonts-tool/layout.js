import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Nepali Fonts Tool', description: 'Type and work with Nepali text using the Everestkit Nepali fonts tool.', path: '/tools/nepali-fonts-tool' });
export default function Layout({ children }) { return children; }
