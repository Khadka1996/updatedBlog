import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Time Zone Converter', description: 'Compare times across cities and time zones with Everestkit.', path: '/tools/time-zone-converter' });
export default function Layout({ children }) { return children; }
