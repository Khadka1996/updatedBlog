import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Split PDF Online', description: 'Split PDF documents into separate pages or files online.', path: '/tools/split-pdf' });
export default function Layout({ children }) { return children; }
