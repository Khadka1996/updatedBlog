import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Edit PDF Online', description: 'Edit PDF files online with Everestkit document tools.', path: '/tools/edit-pdf' });
export default function Layout({ children }) { return children; }
