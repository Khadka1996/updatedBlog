import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'PowerPoint to PDF Converter', description: 'Convert PowerPoint presentations into PDF documents online.', path: '/tools/powerpoint-to-pdf' });
export default function Layout({ children }) { return children; }
