import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Excel to PDF Converter', description: 'Convert Excel spreadsheets to PDF documents online.', path: '/tools/excel-to-pdf' });
export default function Layout({ children }) { return children; }
