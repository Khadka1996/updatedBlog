import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Word Counter', description: 'Count words, characters, and text length online with Everestkit.', path: '/tools/word-counter' });
export default function Layout({ children }) { return children; }
