import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Photo Size Reducer', description: 'Reduce image file sizes online for easier sharing and uploading.', path: '/tools/photo-size-reducer' });
export default function Layout({ children }) { return children; }
