import { createPageMetadata } from '../../seo';
export const metadata = createPageMetadata({ title: 'Photo Cropper', description: 'Crop and prepare images online with the Everestkit photo cropper.', path: '/tools/photo-cropper' });
export default function Layout({ children }) { return children; }
