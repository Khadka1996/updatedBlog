import { createPageMetadata } from '../seo';
export const metadata = createPageMetadata({ title: 'Disclaimer', description: 'Read the Everestkit website and service disclaimer.', path: '/disclaimer' });
export default function Layout({ children }) { return children; }
