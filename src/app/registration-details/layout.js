import { createPageMetadata } from '../seo';
export const metadata = createPageMetadata({ title: 'Registration Details', description: 'View Everestkit company registration and business details.', path: '/registration-details' });
export default function Layout({ children }) { return children; }
