import { createPageMetadata } from '../seo';
export const metadata = createPageMetadata({ title: 'Privacy Policy', description: 'Read how Everestkit collects, uses, and protects personal information.', path: '/privacy-policy' });
export default function Layout({ children }) { return children; }
