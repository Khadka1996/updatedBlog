import { createPageMetadata } from '../../seo';

export const metadata = createPageMetadata({
  title: 'Social Proof Services',
  description: 'Build trust with reviews, testimonials, and social proof experiences for your business.',
  path: '/services/social-proof',
});

export default function Layout({ children }) { return children; }
