import { createPageMetadata } from '../seo';
export const metadata = createPageMetadata({ title: 'Acceptable Use Policy', description: 'Understand the acceptable use requirements for Everestkit tools and services.', path: '/acceptable-use-policy' });
export default function Layout({ children }) { return children; }
