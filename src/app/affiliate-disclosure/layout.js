import { createPageMetadata } from '../seo';
export const metadata = createPageMetadata({ title: 'Affiliate Disclosure', description: 'Read the Everestkit affiliate disclosure and advertising relationship information.', path: '/affiliate-disclosure' });
export default function Layout({ children }) { return children; }
