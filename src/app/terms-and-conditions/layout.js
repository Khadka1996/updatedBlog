import { createPageMetadata } from '../seo';
export const metadata = createPageMetadata({ title: 'Terms and Conditions', description: 'Review the terms and conditions for using Everestkit services and tools.', path: '/terms-and-conditions' });
export default function Layout({ children }) { return children; }
